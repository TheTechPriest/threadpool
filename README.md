# Thread Pool

**A simply thread pool implementation applied to the quicksort algorithm.**

It includes:
- Parallel sorting, dividing vectors by the number of cores
- A testing procedure that averages out results across numerous tests and returns percantage difference
- Variable vector size for the vector to be sorted, reusing the same one from master to cut on run time
- A new and improved benchmarking class which adds inputs to given vectors
